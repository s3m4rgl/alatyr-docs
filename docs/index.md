# Alatyr

Alatyr — платформа для выдачи и управления identity-материалом корпоративных
устройств: клиентские сертификаты для EAP-TLS (Wi-Fi/проводной 802.1X),
mTLS-сертификаты приложений, Windows-доменный вход по смарт-карте и
SSH-доступ. Ключевой материал создаётся и хранится в аппаратном хранилище
устройства — TPM 2.0 (Windows/Linux) или Secure Enclave (macOS) — и никогда
не покидает его; сервер лишь подписывает публичную половину (или,
для SSH Key Registry, регистрирует её как есть, без подписи).

## Что решает Alatyr

- **Wi-Fi / проводной 802.1X (EAP-TLS).** Агент на устройстве сотрудника
  генерирует ключ в TPM/Secure Enclave, отправляет CSR на сервер, ждёт
  одобрения администратором и устанавливает выданный сертификат и
  Wi-Fi/802.1X-профиль — без ручной раздачи сертификатов и паролей.
- **mTLS для внутренних приложений** (`user_mtls`) и **вход в Windows по
  смарт-карте** (`ad_logon`, TPM Virtual Smart Card) — та же модель
  «устройство просит → администратор одобряет → сертификат выдан»,
  независимо для каждой цели.
- **SSH-доступ** — двумя независимыми механизмами: короткоживущие
  OpenSSH-сертификаты через Vault SSH secrets engine и отдельный
  **SSH Key Registry** — реестр «сырых» hardware-backed публичных
  ключей без CA и без сертификата. Устройство уже хранит ключ с
  подтверждением присутствия пользователя в аппаратном хранилище;
  Registry регистрирует публичную половину этого ключа и отдаёт её
  `sshd` через собственный Keyholder API — без выпуска сертификата и
  без CA. Подробности — [SSH Key Registry](ssh-keys.md).

Ни одна заявка не выдаётся автоматически по умолчанию: и сертификаты, и
SSH-ключи попадают в очередь `pending` и требуют явного одобрения человеком.
У двух типов заявок — разные, несимметричные механизмы включения
автоодобрения: для сертификатов это отдельная машинная роль
`cert-auto-approver`, выдаваемая доверенному сервисному аккаунту (см.
[Администрирование](administration.md)); для SSH Key Registry
сервис-аккаунтного пути нет вообще — единственный переключатель —
отдельная настройка автоодобрения, по умолчанию выключенная (см. [SSH
Key Registry](ssh-keys.md)).

## Компоненты

- **Сервер** (Go + PostgreSQL) — единственный компонент с доступом к базе
  и к PKI-бэкендам (HashiCorp Vault и/или внешний SCEP CA). Разворачивается
  один раз на инфраструктуре организации.
- **Агент** (Go, кросс-платформенный CLI) — ставится на каждое устройство
  сотрудника, генерирует ключевой материал в доступном аппаратном
  хранилище и обращается к серверу за сертификатом/регистрацией ключа.
  Поведение по ОС — [Агенты](agents.md).
- **Admin UI** (React) — веб-интерфейс для одобрения заявок, управления
  пользователями/ролями, сетями, сервисными аккаунтами и вебхуками —
  [Администрирование](administration.md).

Полная схема компонентов и потоков данных, включая per-purpose PKI-реестр —
[Архитектура и PKI](architecture.md#pki-ca-per-purpose-registry).

## С чего начать

| Если вы... | Идите сюда |
|---|---|
| Разворачиваете Alatyr впервые | [Установка](installation.md) → [Конфигурация](configuration.md) |
| Планируете продакшен-нагрузку/несколько реплик | [Resource Sizing](sizing.md), [Отказоустойчивость (HA)](ha.md) |
| Администрируете уже развёрнутый инстанс | [Администрирование](administration.md) |
| Разворачиваете агент на устройствах | [Установка](installation.md), [Агенты](agents.md) |
| Настраиваете SSH-доступ через SSH Key Registry | [SSH Key Registry](ssh-keys.md) |
| Интегрируетесь через API | [REST API](api.md) |
| Расследуете инцидент или ищете причину сбоя | [Логирование](logging.md), [Эксплуатация](operations.md), [Траблшутинг](troubleshooting.md) |
